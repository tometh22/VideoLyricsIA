[pytest]
markers =
    umg_smoke: slow end-to-end UMG ProRes render smoke test (skip in CI; run manually before UMG delivery weeks via `pytest -m umg_smoke`)

# Skip umg_smoke by default. To run: pytest -m umg_smoke
addopts = -m "not umg_smoke"
